import PropTypes from 'prop-types';
import {Text} from '@rneui/themed';

const SuggestPlant = ({navigation}) => {
  return <Text>SuggestPlant</Text>;
};

SuggestPlant.propTypes = {
  navigation: PropTypes.object,
};

export default SuggestPlant;
